import 'package:flutter/material.dart';

class AppColors {
  // GRADIENTS
  static const backgroundGradinet = LinearGradient(
    colors: [Color(0xFF203643), Color(0xFF11232F)],
    end: Alignment.bottomCenter,
    begin: Alignment.topCenter,
  );
  static const tintGradient = LinearGradient(
    colors: [Color(0xFF5EB687), Color(0xFF77D596)],
  );
  static const thirdGradient = LinearGradient(
    colors: [Color(0xFF0A664F), Color(0xFF167A65)],
  );

  // COLORS
  static const primaryColor = Color(0xFFFFFFFF);
  static const secondaryColor = Color(0xFF2B3C4F);
  static const thirdColor = Color(0xFF1C5C6C);
  static const fourthColor = Color(0xFF67C28D);
}
